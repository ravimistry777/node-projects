<!DOCTYPE html>
<html>
<head>
  <title>TODO List</title>

  <!-- Fonts: Luxury Clinic Style -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;600&family=Inter:wght@400;500&display=swap" rel="stylesheet">

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      padding: 40px;
      font-family: 'Inter', sans-serif;
      background: #f6f4ef;
      color: #2a2a2a;
    }

    .container {
      max-width: 1000px;
      margin: auto;
      background: #ffffff;
      padding: 40px;
      border-radius: 20px;
      box-shadow: 0 40px 80px rgba(0,0,0,0.08);
      border: 1px solid #e6e2d8;
    }

    h1 {
      font-family: 'Playfair Display', serif;
      text-align: center;
      font-size: 34px;
      font-weight: 600;
      margin-bottom: 10px;
      letter-spacing: 0.5px;
    }

    .subtitle {
      text-align: center;
      font-size: 14px;
      color: #7a7468;
      margin-bottom: 35px;
    }

    .top-bar {
      display: flex;
      justify-content: flex-end;
      margin-bottom: 24px;
    }

    .add-btn {
      background: transparent;
      border: 1.5px solid #b89b5e;
      color: #b89b5e;
      padding: 12px 26px;
      border-radius: 999px;
      text-decoration: none;
      font-weight: 500;
      letter-spacing: 0.4px;
      transition: all 0.25s ease;
    }

    .add-btn:hover {
      background: #b89b5e;
      color: #ffffff;
    }

    .todo-table {
      width: 100%;
      border-collapse: separate;
      border-spacing: 0 18px;
    }

    .todo-table thead th {
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.12em;
      color: #8b8578;
      padding: 0 18px 12px;
      text-align: left;
      font-weight: 500;
    }

    .todo-table tbody tr {
      background: #ffffff;
      border-radius: 18px;
      border: 1px solid #eee9dc;
      transition: all 0.3s ease;
    }

    .todo-table tbody tr:hover {
      box-shadow: 0 18px 40px rgba(0,0,0,0.08);
      transform: translateY(-3px);
    }

    .todo-table td {
      padding: 22px 18px;
      font-size: 15px;
      border: none;
      vertical-align: middle;
    }

    .todo-table td:first-child {
      border-top-left-radius: 18px;
      border-bottom-left-radius: 18px;
      font-weight: 500;
      color: #6f6a60;
    }

    .todo-table td:last-child {
      border-top-right-radius: 18px;
      border-bottom-right-radius: 18px;
    }

    .title-cell {
      font-weight: 500;
      color: #1f1f1f;
    }

    .desc-cell {
      color: #6e6a60;
      line-height: 1.6;
    }

    .action-btn {
      padding: 8px 18px;
      border-radius: 999px;
      font-size: 13px;
      font-weight: 500;
      text-decoration: none;
      transition: all 0.25s ease;
      display: inline-block;
    }

    .edit-btn {
      border: 1px solid #8fa6a0;
      color: #5f7770;
      background: transparent;
    }

    .edit-btn:hover {
      background: #5f7770;
      color: #ffffff;
    }

    .delete-btn {
      border: 1px solid #c17c74;
      color: #a3574f;
      background: transparent;
      margin-left: 6px;
    }

    .delete-btn:hover {
      background: #a3574f;
      color: #ffffff;
    }

    .empty-msg {
      text-align: center;
      padding: 80px 30px;
      background: #faf9f6;
      border-radius: 20px;
      border: 1px dashed #d6cfbf;
      color: #7a7468;
      font-size: 18px;
      font-family: 'Playfair Display', serif;
    }

    @media (max-width: 768px) {
      body {
        padding: 20px;
      }

      .container {
        padding: 26px;
      }

      h1 {
        font-size: 28px;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>📝 TODO List</h1>
    <div class="subtitle">Private • Professional • Discreet Task Management</div>

    <div class="top-bar">
      <a href="/add-todo" class="add-btn">Add New TODO</a>
    </div>

    <% if(todos.length > 0) { %>
      <table class="todo-table">
        <thead>
          <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Description</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% todos.forEach(todo => { %>
            <tr>
              <td><%= todo.id %></td>
              <td class="title-cell"><%= todo.title %></td>
              <td class="desc-cell"><%= todo.description %></td>
              <td>
                <a href="/edit-todo/<%= todo.id %>" class="action-btn edit-btn">Edit</a>
                <a href="/delete-todo/<%= todo.id %>" class="action-btn delete-btn">Delete</a>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } else { %>
      <div class="empty-msg">
        No records available<br><br>
        Please add a new entry to begin
      </div>
    <% } %>
  </div>
</body>
</html>
