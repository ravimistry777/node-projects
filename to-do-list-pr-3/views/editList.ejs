<!DOCTYPE html>
<html>
<head>
  <title>Edit TODO</title>

  <!-- Luxury Clinic Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;600&family=Inter:wght@400;500&display=swap" rel="stylesheet">

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      padding: 40px;
      font-family: 'Inter', sans-serif;
      background: #f6f4ef;
      color: #2a2a2a;
    }

    .container {
      max-width: 520px;
      margin: 60px auto;
      background: #ffffff;
      padding: 40px;
      border-radius: 20px;
      box-shadow: 0 40px 80px rgba(0,0,0,0.08);
      border: 1px solid #e6e2d8;
    }

    h1 {
      font-family: 'Playfair Display', serif;
      text-align: center;
      font-size: 30px;
      font-weight: 600;
      margin-bottom: 10px;
      letter-spacing: 0.4px;
    }

    .subtitle {
      text-align: center;
      font-size: 14px;
      color: #7a7468;
      margin-bottom: 34px;
    }

    .form-group {
      margin-bottom: 22px;
    }

    label {
      display: block;
      margin-bottom: 8px;
      font-size: 13px;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: #8b8578;
      font-weight: 500;
    }

    input,
    textarea {
      width: 100%;
      padding: 14px 16px;
      border-radius: 12px;
      border: 1px solid #ddd7c8;
      font-size: 15px;
      font-family: 'Inter', sans-serif;
      background: #faf9f6;
      transition: border 0.2s, box-shadow 0.2s;
    }

    input:focus,
    textarea:focus {
      outline: none;
      border-color: #b89b5e;
      box-shadow: 0 0 0 3px rgba(184,155,94,0.15);
      background: #ffffff;
    }

    textarea {
      height: 110px;
      resize: vertical;
    }

    .btn-group {
      display: flex;
      gap: 12px;
      margin-top: 34px;
    }

    .update-btn,
    .cancel-btn {
      flex: 1;
      padding: 13px;
      border-radius: 999px;
      font-size: 15px;
      font-weight: 500;
      text-align: center;
      text-decoration: none;
      cursor: pointer;
      transition: all 0.25s ease;
    }

    .update-btn {
      background: transparent;
      border: 1.5px solid #5f7770;
      color: #5f7770;
    }

    .update-btn:hover {
      background: #5f7770;
      color: #ffffff;
    }

    .cancel-btn {
      background: transparent;
      border: 1.5px solid #c17c74;
      color: #a3574f;
    }

    .cancel-btn:hover {
      background: #a3574f;
      color: #ffffff;
    }

    @media (max-width: 600px) {
      body {
        padding: 20px;
      }

      .container {
        padding: 28px;
      }

      h1 {
        font-size: 26px;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>✏️ Edit TODO</h1>
    <div class="subtitle">Private • Professional • Discreet Update</div>

    <form action="/update-todo/<%= todo.id %>" method="POST">
      <div class="form-group">
        <label for="id">ID</label>
        <input type="text" id="id" name="id" value="<%= todo.id %>" required>
      </div>

      <div class="form-group">
        <label for="title">Title</label>
        <input type="text" id="title" name="title" value="<%= todo.title %>" required>
      </div>

      <div class="form-group">
        <label for="description">Description</label>
        <textarea id="description" name="description" required><%= todo.description %></textarea>
      </div>

      <div class="btn-group">
        <button type="submit" class="update-btn">Update TODO</button>
        <a href="/" class="cancel-btn">Cancel</a>
      </div>
    </form>
  </div>
</body>
</html>
