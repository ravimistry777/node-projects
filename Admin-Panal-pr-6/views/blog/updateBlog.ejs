<%- include('../header') %>

<div class="page-content py-4">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-5 fade-in-up">
        <div>
            <div class="d-flex align-items-center gap-2 mb-1">
                <a href="/blog/view-blogs" class="text-muted text-decoration-none hover-dark">
                    <i class="bi bi-arrow-left"></i> Blogs
                </a>
                <span class="text-muted">/</span>
                <span class="text-dark fw-medium">Edit</span>
            </div>
            <h4 class="fw-bold m-0 text-dark">Update Blog Post</h4>
        </div>
    </div>

    <div class="row justify-content-center fade-in-up" style="animation-delay: 0.1s;">
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm">
                <div class="card-body p-5">
                    <form method="post" action="/blog/update-blog" enctype="multipart/form-data">
                        <input type="hidden" name="id" value="<%= blog._id %>">
                        
                        <!-- Section: Details -->
                        <div class="mb-5">
                            <h6 class="text-uppercase text-muted fw-bold mb-4" style="font-size: 0.75rem; letter-spacing: 0.05em;">Post Details</h6>
                            
                            <div class="row g-4">
                                <div class="col-12">
                                    <label class="form-label small fw-medium text-secondary">Title</label>
                                    <input type="text" class="form-control bg-light border-0" name="title" value="<%= blog.title %>" required style="padding: 10px 12px;">
                                </div>
                                
                                <div class="col-md-6">
                                    <label class="form-label small fw-medium text-secondary">Author</label>
                                    <input type="text" class="form-control bg-light border-0" name="author" value="<%= blog.author %>" required style="padding: 10px 12px;">
                                </div>
                            </div>
                        </div>

                        <!-- Section: Content -->
                        <div class="mb-5">
                            <h6 class="text-uppercase text-muted fw-bold mb-4" style="font-size: 0.75rem; letter-spacing: 0.05em;">Content</h6>
                            <div class="col-12">
                                <label class="form-label small fw-medium text-secondary">Body</label>
                                <textarea name="content" class="form-control bg-light border-0" rows="8" required style="padding: 12px; resize: vertical;"><%= blog.content %></textarea>
                            </div>
                        </div>

                        <!-- Section: Cover Image -->
                        <div class="mb-5">
                            <h6 class="text-uppercase text-muted fw-bold mb-4" style="font-size: 0.75rem; letter-spacing: 0.05em;">Cover Image</h6>
                            <div class="d-flex align-items-start gap-4">
                                <div class="position-relative">
                                    <% if(blog.blogImage) { %>
                                        <img src="<%= blog.blogImage %>" alt="current cover" class="rounded-3 border" width="120" height="80" style="object-fit: cover;">
                                    <% } else { %>
                                        <div class="d-flex align-items-center justify-content-center bg-light rounded-3 text-muted" style="width: 120px; height: 80px; border: 1px dashed var(--border-focus);">
                                            <i class="bi bi-image fs-4"></i>
                                        </div>
                                    <% } %>
                                </div>
                                <div class="flex-grow-1">
                                    <input type="file" name="blogImage" class="form-control bg-light border-0" style="padding: 10px 12px;">
                                    <div class="form-text text-muted small mt-2">Upload to replace current image. Recommended: 1200x630px.</div>
                                </div>
                            </div>
                        </div>

                        <hr class="border-light my-5">

                        <div class="d-flex justify-content-end gap-3">
                            <a href="/blog/view-blogs" class="btn btn-light px-4 text-secondary">Cancel</a>
                            <button type="submit" class="btn btn-dark px-4">Save Changes</button>
                        </div>

                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('../footer') %>
